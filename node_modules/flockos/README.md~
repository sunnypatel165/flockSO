# FlockOS SDK for node.js

# Installation

```
npm install talk-to/flockos-node-sdk
```

# Usage

First, `require` the module, then set your app id and app secret.

```
var flock = require('flockos');

flock.setAppId('<app id>');
flock.setAppSecret('<app secret>');
```